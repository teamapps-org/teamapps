{
  "name": "teamapps-client-communication",
  "version": "1.0.0",
  "main": "dist/js/main.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "del_dist": "rm -rf dist/*",
    "clean": "yarn run del_dist",
    "tsc": "tsc -b -v",
    "build": "npm run tsc",
    "watch": "yarn run del_dist && tsc --watch"
  },
  "devDependencies": {
    "cross-var": "^1.1.0",
    "esbuild": "^0.15.13",
    "replace-in-file": "^6.3.5",
    "typescript": "^4.8.4"
  }
}
