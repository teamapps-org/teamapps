package "projector-combobox":org.teamapps.projector.components.trivial.dto;

import org.teamapps.projector.dto.DtoTemplate;
import org.teamapps.projector.dto.DtoIdentifiableClientRecord;

class DtoTreeRecord extends DtoIdentifiableClientRecord {
	Integer parentId;
	boolean expanded;
	boolean lazyChildren;
	boolean selectable;
	DtoTemplate* displayTemplate; // null == use toString()
}