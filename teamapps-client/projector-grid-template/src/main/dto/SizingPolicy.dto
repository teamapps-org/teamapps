package "projector-grid-template":org.teamapps.projector.template.grid;

class DtoSizingPolicy {
	required DtoSizeType type;
    double value;
    int minAbsoluteSize;
}

enum DtoSizeType {
	FIXED = "px",
	RELATIVE = "%",
	AUTO = "auto",
	FRACTION = "fr"
}