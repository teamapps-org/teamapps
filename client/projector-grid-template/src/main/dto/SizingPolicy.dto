package "projector-grid-template":org.teamapps.projector.template.grid;

class DtoSizingPolicy {
	SizeType type;
    double value;
    int minAbsoluteSize;
}

enum SizeType {
	FIXED = "px",
	RELATIVE = "%",
	AUTO = "auto",
	FRACTION = "fr"
}
