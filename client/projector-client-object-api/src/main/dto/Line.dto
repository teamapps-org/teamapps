package "projector-client-object-api":org.teamapps.projector.format;

class DtoLine {
	double thickness;
	String color;
	LineType type;
}

enum LineType {
	SOLID = "solid",
	DOTTED = "dotted",
	DASHED = "dashed",
	DOUBLE = "double"
}
