{
  "name": "projector-notification-bar",
  "version": "0.1.0-SNAPSHOT",
  "type": "module",
  "exports": "./target/js-dist/index.js",
  "types": "./target/js-dist/lib/index.d.ts",
  "scripts": {
    "del_dist": "rimraf 'dist/*'",
    "clean": "yarn run del_dist",
    "tsc": "tsc",
    "bundle": "node esbuild.mjs",
    "build": "yarn run del_dist && yarn run tsc && yarn run bundle"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "rimraf": "^3.0.2",
    "typescript": "^5.1"
  },
  "dependencies": {
    "projector-client-object-api": "workspace:*",
    "projector-progress-indicator": "workspace:*"
  }
}
